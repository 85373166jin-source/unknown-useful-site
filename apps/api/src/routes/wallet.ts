import {Hono,type Context} from 'hono';import{z}from'zod';import type{AppEnv}from'../middleware/auth';import{bearerAuth}from'../middleware/auth';import{ApiError}from'../middleware/error';import{createWithdrawal,getWallet,listNotifications,listWithdrawals,markNotificationsRead}from'../services/wallet';async function json(c:Context<AppEnv>){try{return await c.req.json()}catch{throw new ApiError('invalid_json','Request body must be valid JSON',400)}}const schema=z.object({amountCents:z.number().int().positive(),method:z.enum(['wechat','alipay']),account:z.string().min(1).max(200),note:z.string().max(500).optional()});export const walletRoutes=new Hono<AppEnv>();walletRoutes.use('*',bearerAuth);walletRoutes.get('/',async c=>c.json({entries:await getWallet(c.env,c.get('userId'))}));walletRoutes.get('/notifications',async c=>c.json({notifications:await listNotifications(c.env,c.get('userId'))}));walletRoutes.post('/notifications/read',async c=>{await markNotificationsRead(c.env,c.get('userId'));return c.json({ok:true})});walletRoutes.get('/withdrawals',async c=>c.json({withdrawals:await listWithdrawals(c.env,c.get('userId'))}));walletRoutes.post('/withdrawals',async c=>{const p=schema.safeParse(await json(c));if(!p.success)throw new ApiError('invalid_request','Request validation failed',400);return c.json(await createWithdrawal(c.env,c.get('userId'),p.data),201)});
