# The public API origin. Leave as the default to use the Vite dev proxy,
# which forwards local /api requests to http://127.0.0.1:8787.
# For cross-origin deployments set an absolute URL, for example:
# VITE_API_BASE_URL=https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1
